@{
    ViewBag.Title = "Listă categorii";
}

<br />
<br />

<h2 class="text-center">@ViewBag.Title</h2>

<div class="container my-4">


@if (ViewBag.Message != null)
{
        <div class="alert @ViewBag.Alert" role="alert">@ViewBag.Message</div>
}


<br />
<br />

    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-2 row-cols-lg-4 g-4">


        @foreach (Proiect_DAW_2025.Models.Category category in ViewBag.Categories)
        {
                    <div class="col">
                        <div class="card h-100 product-card">
                            <div class="card-body d-flex flex-column">
                                <h6 class="card-title">@category.CategoryName</h6>

                                <a asp-controller="Categories"
                                   asp-action="Show"
                                   asp-route-id="@category.Id"
                                   class="btn btn-primary mt-2">

                                    Afișează categoria
                                </a>

                                <a asp-controller="Categories"
                                   asp-action="Edit"
                                   asp-route-id="@category.Id"
                                   class="btn btn-primary mt-2">

                                    Editează categoria
                                </a>
                            </div>
                        </div>
                    </div>

        }

    </div>
</div>

<br />
<a class="btn btn-primary" asp-controller="Categories" asp-action="New">Adaugă o nouă categorie</a>
<br />

